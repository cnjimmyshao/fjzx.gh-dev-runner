# 项目 Agent 协作准则

适用于 `cnjimmyshao/fjzx.gh-dev-runner` 的规划、需求整理、编码、Review、修复、协调及委派的子 Agent。

**以最简单的正确实现完成当前能力。对业务结果严格，对实现复杂度克制；不把消除所有 Review 评论或封堵所有理论错误用法当作目标。**

## 语言与角色

文档、Issue、PR、Review 与交接采用中文叙述和统一英文专业术语。Contract 指数据、接口或行为保证；Requirement 是需求，Implementation 是实现方式；Finding 是待核实的审查发现，不自动等于必须执行的指令。标识符、路径、日志原文不因术语整理而改名。

角色按任务使命和授权确定，不绑定模型、连接器或执行电脑。

| 角色 | 使命 | 边界 |
| --- | --- | --- |
| Coordinator（规划／协调者） | 梳理需求、编写与拆分 Issue、组织决策、核对交付。 | 区分建议与获批决定，不因主持讨论取得维护者权限。 |
| Implementer（实现者） | 按确认要求编码、测试、修复并交付证据。 | 普通实现取舍自行决定，不擅改需求、保障范围或验收。 |
| Reviewer（审查者） | 核对当前 head、Contract、正确性、证据及复杂度。 | 不把个人偏好升级成需求，不自行修复或合并。 |
| Maintainer（维护者） | 决定范围、业务语义、重要取舍与放行。 | 由人或明确授权的代理在授权范围内承担。 |

不要求为每个角色新增独立 Agent 或人工审批阶段。同一 Agent 可按分工切换角色；工具能力不等于授权，只读观察不自动升级为修改，实现者自查不替代独立 Review。

## 开始或恢复任务

实际读取本文件、[文档导航](docs/README.md)、相关 Current、关联 Issue 全文与最新问题／决定、现有 PR 的 Review 和修复记录；环境要求见 [开发说明](docs/development.md)。不要假设另一会话已经传入上下文。

AGENTS 管工作方法，Current 管目标行为，Issue 及已确认决定管本次范围。旧代码、旧测试、其他任务方案和 Review 建议不反向定义需求。研究证据可促成修订，但不能自动覆盖 Current；发现冲突按决策交接处理。

## Issue → PR 开发入口

实质开发或需求、接口、行为、权限、运行方式改变，先有 Issue，再建分支和 PR：

```text
Issue → 确认 Scope / Contract → 必要时 Research → 分支
→ 实现 + Tests + 必要文档 → PR → Review → 必要修复与复审
→ 维护者批准 Merge → Close Issue
```

### Contract 文档先行

改变长期 Requirement、Contract、职责边界、运行流程、权限、对外行为保证或验收语义时，先在关联 Issue 中形成决定，再通过**独立的 documentation-only PR**更新对应的 Current、AGENTS 或必要 ADR。文档 PR 完成 Review 并合并后，该规则才成为当前实现依据；随后再开或继续独立的 Implementation Issue / PR，由实现者重新读取最新 main 后编码、测试和 Review。不要把尚未合并的候选文档当作已生效规范，也不要在代码 PR 中顺手制定新的 Contract。

后续若实现过程中发现必须改变已经合并的 Contract，同样先回到关联 Issue 说明原因与影响，再用独立文档 PR 修改并合并，然后继续代码调整。这样让“为什么改规则”“规则何时生效”“哪次代码实现依据哪版规则”都能从 Issue / PR / Git 历史追溯。

普通实现细节仍由 Implementer 在已确认 Contract 内自主决定，不为函数、内部结构或每个配置字段增加文档审批。纯拼写、格式、失效链接等不改变语义的文档维护沿用轻量例外；紧急绕过文档先行必须由 Maintainer 明确授权，并在事后补齐可追踪记录。

已有任务直接复用，不重复建 Issue。纯拼写、格式等不改变行为的维护可以不单独建 Issue，但仍通过 PR。特殊紧急处理需维护者明确授权并补记录，不能因改动小就直接写 main。

最终交付 PR 使用 `Closes #N` 关联本仓库任务；中间交付使用 `Refs #N`，不得提前关闭未完成任务。没有明确授权，不自动 merge、部署、迁移、删除真实数据或关闭其他任务。模板是入口提示，不是额外审批制度。

### 编写与拆分 Issue

Coordinator 写清问题、目标、包含与不包含的范围、依据和可观察的验收结果。启动所需的任务特有条件写在 Issue，不依赖聊天中的第二份规范。

Requirement 与 Implementation 分开；实现建议可写，但未批准的方案不得写成必须采用的需求。理论边界、未来扩展及底层库存在的 API 不自动成为 Scope。会影响语义、接口、责任或验收的未决项明确保留。

仅在可以独立实现、验收、交付时拆子任务；不为颗粒度制造大量小 Issue。lint、tests 和 Review 状态是交付检查，不单独替代业务验收。小任务几句话即可，引用长期规则而不复制整套规则。

## 实现原则

- **能力与整体简单。** 先保证正常路径与承诺的异常处理。按理解、测试、维护成本选择方案；简单函数足够时不新增架构层、配置系统、消息队列、多模型适配或自动恢复平台。
- **责任明确。** 区分开发者应遵守的规范与底层必须强制保证的能力；不因“必须／不得”措辞自动构建全面防误用框架。结构校验、业务行为、并发一致性和安全边界分别放在适当责任层。
- **风险相称。** 内部受控调用不预设为恶意绕过；授权、网络输入、评论到本机执行、凭证与文件访问等实际边界仍须保护。单机不代表没有并发，必要去重和单写入者保障不能因简单而删除。
- **尊重取舍。** 不把当前不做的功能或允许的人工恢复变成隐藏的自动化承诺；也不把未确认假设写成已接受限制。保留正常业务能力，不为了数据整齐或审查通过增加未经确认的限制。
- **测试与诚实。** 覆盖成功路径、关键非法输入／状态和实际并发。按改动选择验证范围，不为小改动无条件重跑昂贵全链路。未运行、跳过、环境失败、自报与独立验证分开记录。

这是可触发本机工作的工具。公开评论不得直接作为 shell 命令执行；真实凭证和私有任务内容不得进入公开日志。隔离测试会话、目录和真实工作；没有授权不安装、升级、重启或接管已有 Harness。已有保护的迁移须说明替代责任、受支持路径及验证，不以“简单优先”作为绕过实际安全要求的理由。

## 代码与文档同步

先核对相关 Current。Implementation 只是落实已经合并的 Contract 时，不制造无意义文档 diff；可以在同一 Implementation PR 中同步与该实现直接相关、但**不改变 Contract**的实现说明、示例、开发文档或链接。

如果实现过程中发现必须改变长期 Requirement、Contract、职责边界、运行流程、接口、权限、对外行为保证或验收语义，不在当前 Implementation PR 中顺手修改 Current 来制定新规则。应回到关联 Issue 说明原因与影响，先通过独立的 documentation-only PR 更新对应 Current、AGENTS 或必要 ADR，并在该文档 PR Review / Merge 后，再回到 Implementation PR 继续实现。重要且易反复争论的取舍记录 ADR；新的外部事实或本机实测按 Research 规则留证。

Issue 中获批的新长期规则必须通过上述独立文档 PR 落实并先合并，不能留待未来补写，也不能由 Implementation PR 先行生效。不得为通过审查而反向降低需求、删除有效测试或把既有承诺改称不支持。Current 的目标 Contract 不等于功能已经实现；实现和验证状态必须如实说明。

## Code Review Rules

Reviewer 与处理 Finding 的 Implementer 使用同一标准：

- 核对相关 Current、Issue 及已确认决定和当前 head。缺陷说明运行前提、实际可达或承诺支持的路径、违反的要求与具体后果；清楚的代码分析可以构成证据，不要求先在线上发生。
- 分别判断现象是否成立、是否违反已确认要求、所建议修法是否必要。区分真实缺陷、设计质疑、可选增强与偏好；现象成立不自动推出某一种方案必须采用。
- 同根因尽量归并，检查责任错位、重复规则、补丁叠加、正常能力被误伤和不必要通用化。横向检查只覆盖相关根因，不借机要求无关重构；局部修复足够就不要求重写。
- 真实正确性、安全性、兼容性或正常运行问题不能以简单为由忽略。复杂度意见同样应说明实际影响，不设 Finding 数量上限，不伪造无问题结论。
- 需要改变范围、语义、保障或验收的方案交维护者决定，Reviewer 不直接制定需求。非阻塞增强不作为合并条件；涉及未决前提的结论标明适用条件。

## 修复与最终交付

确认真实缺陷后按根因修复、补相关测试和必要文档，不按 P1/P2/P3 标签机械加代码。Finding 前提不成立可用证据回应而不改生产代码；额外增强另说明必要性。重复同根因或特判不断增加时，先审视责任边界，不无限重试也不忽略缺陷收尾。

同一 PR 分支由一个执行者协调写入，形成完整一批修改后再送审；自动审查已运行时不重复触发。核对 reviewed commit 与当前完整 head；无评论、空 checks、旧 SHA 的审查、进程成功退出或执行者自报，不能冒充当前已独立验证通过。基础设施故障与代码缺陷、任务阻塞分别记录。

交付前更新 PR 正文的最终摘要：当前 head、实现与必要改动理由、测试和 Review 的实际版本与结果、剩余限制，以及关联 Issue 的未决项链接。保留历史评论，不复制完整日志；没有证据就明确未验证。简单任务几句话即可，不要求设计论文。

## 决策交接与完成标准

普通实现取舍由 Implementer 在确认要求内决定。需要澄清、协助或维护者决定的问题，统一在关联 Issue 及时提出、汇总并记录状态。普通代码缺陷、修复与验证仍在 PR 线程；从 PR 发现的待决事项回贴 Issue 并链接原讨论，PR 只引用，不维护另一套待决清单或决定。

发现者说明依据／歧义、建议和理由、影响及阻塞范围、当前实现是否依赖该选择。Coordinator 整理为可直接决定的问题，不是只列“请确认”。必要时可做小范围候选验证，不把候选方案或维护者未回应视为获批；不受影响的工作继续。

Maintainer 给出明确结论或有限探索授权，Coordinator 将决定记录在关联 Issue。Implementer 实际读取后落实必要文档、代码与测试，Reviewer 核对决定和验证，交接者更新该项状态。不要新建重复决策文档，也不要求人逐项批准普通实现取舍。

分别说明实现、Review 与验收状态。影响当前验收的未决／未落实事项不得标为完成；候选实现、测试通过或 Review 无 Finding 都不替代维护者决定。无关可选增强不阻塞交付，不强迫逐项回答。

## 各类 Agent 的接入与规则版本

根 AGENTS 是本仓库共享工作方法的单一维护位置。支持自动加载的工具使用它；其他 runner／Agent 在启动时显式读取并传入，子任务携带同一规则版本、相关 Contract、职责与授权。

启动消息可以只指向仓库和 Issue，但不能省略实际加载。规则更新后，已有会话、旧分支及外部 runner 必须重读或明确传入新版本；文件已合并不证明所有会话已收到。Review 未配置或无法启动时如实报告，不为制造绿色状态添加 Workflow 或空提交。

Current 始终从 `docs/current/` 读取，不建版本化当前目录；如采用版本号，只在其 README 声明并由维护者决定升级。Research 不自动覆盖 Current，Archive 不作为当前依据。完整目录分工见文档导航。
